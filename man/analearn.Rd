% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analearn2.R
\name{analearn}
\alias{analearn}
\title{Analyze learning index (LI) and genotype differences via bootstrap}
\usage{
analearn(
  data,
  nboot = 10000,
  naivelevel = "N",
  seed = NA,
  controlGenotype = NULL,
  method = c("mean", "median"),
  conf = 0.95
)
}
\arguments{
\item{data}{Data frame with columns: genotype, condition, CI.}

\item{nboot}{Number of bootstrap resamples (default 10000).}

\item{naivelevel}{Which condition level is "naive" baseline (default "N").}

\item{seed}{Optional seed for reproducibility (default NA).}

\item{controlGenotype}{Reference genotype for inference (default NULL).}

\item{method}{Aggregation function for CI within genotype/condition: "mean" or "median".}

\item{conf}{Confidence level for bootstrap CI (default 0.95).}
}
\value{
A list with vectors genotype, LI, p and full results data.frame in $data.
}
\description{
Computes learning index (LI) per genotype and trained condition:
LI = (CInaive - CItrained) / CInaive
}
\details{
If controlGenotype is provided, computes bootstrap confidence intervals
and two-sided p-values for the difference in LI versus control:
(LI_control - LI_genotype).
}
